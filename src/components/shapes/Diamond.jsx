import React from 'react'
import {Diamond} from "react-konva"

export default function Diamond({diamonds}) {

 
 
  return (
    <Layer>

        {
            diamonds.map((diamond, index) => {

             return (

             )

            })
        }

    </Layer>
  )
}
